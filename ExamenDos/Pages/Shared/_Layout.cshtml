<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />
    <title>@ViewData["Title"] - Examen #2</title>
    @await Html.PartialAsync("_HeaderPartial");

</head>
<body>

    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">Examen #2</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    @*<li class="nav-item">
                        <a class="nav-link active" aria-current="page"  asp-page="~/Inicio/Index">Inicio</a>
                    </li>*@
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" asp-page="/Producto/Grid">Productos</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" asp-page="/Orden/Grid">Ordenes</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    @RenderBody()

    @await Html.PartialAsync("_ScriptsPartial");
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
